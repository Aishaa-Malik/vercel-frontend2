import React from 'react';
import UnifiedRevenuePage from '../common/UnifiedRevenuePage';

const TurfRevenuePage: React.FC = () => {
  return <UnifiedRevenuePage serviceType="turf" />;
};

export default TurfRevenuePage;

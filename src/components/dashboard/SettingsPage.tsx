import React from 'react';
import UnifiedSettingsPage from '../common/UnifiedSettingsPage';

const SettingsPage: React.FC = () => {
  return <UnifiedSettingsPage serviceType="doctor" />;
};

export default SettingsPage;